apiVersion: 1

# Datasource provisioning for Grafana
# Automatically configures Prometheus as a datasource on container startup

datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    editable: true
    jsonData:
      timeInterval: 15s
      alertmanagerUid: prometheus
