/**
 * Minimal type stub for @keyv/redis.
 * Replaced by the real package types once `pnpm install` is run.
 */
declare module '@keyv/redis' {
  export default class KeyvRedis {
    constructor(uri: string, options?: Record<string, unknown>);
  }
}
